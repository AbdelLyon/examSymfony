<nav class="navbar navbar-expand-lg navbar-light bg-white shadow">
	<div class="container-fluid mx-3">
		<a class="navbar-brand" href="{{ path('film_index') }}">
			<span class="fst-italic">Blog</span>
		</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse h-100 flex-row-reverse" id="navbarNav">
			<ul class="navbar-nav">
				{% if app.user %}
					<li class="nav-item">
						<a class="nav-link" href="{{ path('film_new') }}">Ajouter</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{ path('user_logout') }}">
							Deconnexion
						</a>
					</li>
				{% else %}
					<li class="nav-item me-4">
						<a class="nav-link" href="{{ path('user_register') }}">
							Inscription
						</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{ path('user_login') }}">Connexion</a>
					</li>
				{% endif %}
			</ul>
		</div>
	</div>
</nav>
